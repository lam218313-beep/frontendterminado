# =============================================================================
# Pixely Partners Backend - Environment Variables
# =============================================================================
# Copia este archivo a .env y configura los valores
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Base de Datos (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_USER=pixely_user
POSTGRES_PASSWORD=tu_password_seguro_aqui
POSTGRES_DB=pixely_db

# -----------------------------------------------------------------------------
# API Admin (Usuario que se crea automáticamente al iniciar)
# -----------------------------------------------------------------------------
ORCHESTRATOR_USER=admin@tuempresa.com
ORCHESTRATOR_PASSWORD=tu_password_admin

# -----------------------------------------------------------------------------
# APIs de IA
# -----------------------------------------------------------------------------
# Gemini (REQUERIDO para análisis Q1-Q10)
GEMINI_API_KEY=tu_gemini_api_key

# OpenAI (OPCIONAL - para análisis legacy)
OPENAI_API_KEY=tu_openai_api_key
OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Opcional: Si cambias estos, también cambia en docker-compose.yml
# -----------------------------------------------------------------------------
# DATABASE_URL se construye automáticamente a partir de las variables de arriba
